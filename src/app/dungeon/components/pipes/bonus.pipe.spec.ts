/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { BonusPipe } from './bonus.pipe';

describe('Pipe: Bonuse', () => {
  it('create an instance', () => {
    let pipe = new BonusPipe();
    expect(pipe).toBeTruthy();
  });
});
